# Ambiente virtual Python (recriado por iniciar.command / iniciar.bat)
venv/
__pycache__/
*.pyc

# Config do painel admin: pode conter apiToken / remoteToken.
# O app recria com os valores padrão se o arquivo não existir.
admin/config.json

# Credencial de publicação. NUNCA versionar: o repositório é público, e quem
# tiver esta chave pode publicar no catálogo. Ela viaja apenas dentro do
# pacote .zip entregue a cada pessoa.
chave/

# macOS
.DS_Store
